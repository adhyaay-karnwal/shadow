import { task } from "@trigger.dev/sdk";

export const githubWebhookPullRequest = task({
  id: "github-webhook-pull-request",
  run: async (payload) => {
    // TODO: Implement GitHub webhook pull request logic
  },
});