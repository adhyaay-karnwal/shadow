import { task } from "@trigger.dev/sdk";

export const createTaskPullRequest = task({
  id: "create-task-pull-request",
  run: async (payload) => {
    // TODO: Implement create task pull request logic
  },
});